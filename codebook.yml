# CodeBook Configuration
# Run with: codebook run

main_dir: codebook
exec: true
recursive: true
timeout: 10.0
cache_ttl: 60.0

backend:
  url: http://localhost:3000
  start: true  # Start mock server automatically

cicada:
  enabled: true
  url: http://localhost:9999
  start: true  # Start cicada server automatically
